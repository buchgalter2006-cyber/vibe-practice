# Образец переменных окружения. Скопируйте в .env и заполните (секреты — только в .env!)
PORT=5000
DATA_SOURCE=mock
APP_DB=data/app.db

# Источники данных — подключаются в фазе 4 (сейчас пустые)
GOOGLE_SHEET_URL=
EXCEL_PATH=
FINTABLO_TOKEN=
BITRIX_WEBHOOK_URL=
